import axios from '@/lib/axios';
import {
  Get{{camelCase module}}OptionsTypes,
  Create{{camelCase module}}OptionsTypes,
  Update{{camelCase module}}OptionsTypes,
  Delete{{camelCase module}}OptionsTypes,
  Get{{camelCase module}},
  Create{{camelCase module}},
  Update{{camelCase module}},
} from './types';

export const get{{properCase module}}s = (params: Get{{properCase module}}OptionsTypes): Promise<Get{{properCase module}}Types[]> => {

  return axios.authorized().get(`/{{lowerCase module}}`, { params });
};

export const get{{properCase module}} = ({ id }: Get{{properCase module}} ): Promise<Get{{properCase module}}Types> => {
  return axios.authorized().get(`/{{lowerCase module}}/${id}`);
};

export const create{{properCase module}} = ({ data }: Create{{properCase module}}OptionsTypes): Promise<Create{{properCase module}}Types> => {
  return axios.authorized().post(`/{{lowerCase module}}`, data);
};

export const update{{properCase module}} = ({ id, data }: Update{{properCase module}}OptionsTypes): Promise<Update{{properCase module}}Types> => {
  return axios.authorized().patch(`/{{lowerCase module}}/${id}`, data);
};

export const delete{{properCase module}} = ({ id }: Delete{{properCase module}}OptionsTypes) => {
  return axios.authorized().patch(`/{{lowerCase module}}/${id}`);
};

