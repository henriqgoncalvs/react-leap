import { Box, Text } from '@chakra-ui/react';

export type {{properCase component}}Props = {};

export const {{pascalCase component}} = (props: {{properCase component}}Props) => {
  return (
    <Box>
      <Text>{{pascalCase component}}</Text>
    </Box>
  );
};
